import lookAtSS
import keyboard
'''
import pandas as pd

# Load the Excel file
df = pd.read_excel('Overwatch Hero Ult Cost.xlsx')

# Ensure the columns you want to convert exist
if 'Key' in df.columns and 'Value' in df.columns:
    # Convert columns to dictionary
    result_dict = dict(zip(df['Key'], df['Value']))

    print(result_dict)
else:
    print("The specified columns do not exist in the Excel file.")
'''

keyboard.wait('tab')
print("tab pressed")
lookAtSS.main()
